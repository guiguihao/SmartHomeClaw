# 心跳机制配置

heartbeat:
  enabled: true
  # 检查间隔 (cron 表达式)
  interval: "*/5 * * * *"  # 每5分钟
  
  # 检查项
  checks:
    - name: "系统健康"
      prompt: "系统自检：所有设备和服务是否正常？"
      
    - name: "环境优化"
      prompt: "根据当前时间和环境数据，是否需要调节设备？"
      
    - name: "安全巡检"
      prompt: "安全检查：门窗、烟雾、燃气等是否正常？"
